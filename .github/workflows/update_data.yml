name: Scheduled data update

on:
  schedule:
    - cron: "17 6 * * 1,4" # twice weekly (Mon/Thu 06:17 UTC): CFPB + verification refresh
  workflow_dispatch: {}

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest tests/ -q

      - name: Run pipeline (live collectors)
        env:
          REDDIT_CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
          REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
        run: python -m backend.pipeline

      - name: Upload database + logs as artifact
        uses: actions/upload-artifact@v4
        with:
          name: broker-index-db
          path: |
            data/processed/broker_index.db
            logs/pipeline.log
          retention-days: 30

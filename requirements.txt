# Core
# pandas 3.x + pyarrow 25 currently segfault inside libarrow when Streamlit serializes
# dataframes — pin to the battle-tested 2.x/22.x pairing until upstream stabilizes.
pandas>=2.2,<3
pyarrow>=17,<23
requests>=2.32
jinja2>=3.1.5      # pandas Styler (highlighted comparison tables)

# Frontend
streamlit>=1.36
plotly>=5.22

# Collectors
beautifulsoup4>=4.12
lxml>=5.2
praw>=7.7           # Reddit API (optional; used only if credentials are configured)
# playwright>=1.45  # optional: selective JS-rendered collection; install + `playwright install chromium` if needed

# Testing
pytest>=8.2
